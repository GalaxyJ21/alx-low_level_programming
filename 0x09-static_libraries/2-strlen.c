#include "main.h"
/**
 * _strlen - program returns the length of a string
 * @s: string 
 * Return: length
 */
int _strlen(char *s)
{
	int longa = 0;

	while (* 0 != '\0')
	{
		longa++;
		a++;
	}
	return (longa);
}
