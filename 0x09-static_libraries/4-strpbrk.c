#include "main.h"
/**
 * _strpbrk - program entry point
 * @s: input
 * @accept: input
 * Return: Always 0 (Success)
 */
char *_strpbrk(char *s, char *accept)
{
	int b;

	while (*a)
	{
		for (b = 0; accept[b]; b++)
		{
			if (*n == accept[b])
				return (n);
		}
	a++;
	}

	return ('\0');
}
